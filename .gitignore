# 构建产物与分发二进制
/dist/
/downloads/

# 依赖与 vendored 代码（由 build.ps1 抓取 / 构建，不入库）
/node_modules/
/app/dsh/node_modules/
/app/dsh/lib/

# 随包分发的运行时与二进制（由 build.ps1 下载 / 编译，不入库）
/app/runtime/
/app/*.dll
/app/DeepSeek Harness.exe

# 系统 / IDE
.DS_Store
Thumbs.db
